services:
  - type: web
    name: trello-mcp
    runtime: docker
    envVars:
      - key: USE_CLAUDE_APP
        value: "false"
      - key: TRELLO_API_KEY
        sync: false
      - key: TRELLO_TOKEN
        sync: false
